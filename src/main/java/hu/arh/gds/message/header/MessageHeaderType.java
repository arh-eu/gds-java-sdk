package hu.arh.gds.message.header;

/**
 * Indicates the type of the Header.
 * Since clients can only receive {@link MessageHeaderType#BASE} headers, this is the only value listed.
 */
public enum MessageHeaderType {
    BASE
}
